DB_HOST=host.docker.internal
DB_PORT=5432
DB_NAME=tesouraria
DB_USER=tesoureiro
DB_PASSWORD=tesoureiro_secret

APP_PORT=8099

# Fuso horário dos containers (opcional; se vazio, usa /etc/localtime do servidor)
# TZ=America/Sao_Paulo

# Autenticação (API)
JWT_SECRET=
JWT_TTL_SECONDS=28800
GOOGLE_CLIENT_ID=
CADASTRO_ABERTO=true
